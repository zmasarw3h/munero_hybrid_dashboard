# Backend
OLLAMA_BASE_URL=http://ollama:11434
OLLAMA_MODEL=qwen2.5-coder:7b
# For the Compose demo stack, the backend reads from the `client_data` named volume.
# The `seed_db` service copies `munero-platform/data/munero.sqlite` into the volume
# on first run.
DB_FILE=/data/munero.sqlite
CORS_ORIGINS=http://localhost
DEBUG=false
DEBUG_LOG_PROMPTS=false

# Frontend
NEXT_PUBLIC_API_URL=http://localhost

# Caddy
DEMO_DOMAIN=localhost
ACME_EMAIL=admin@yourdomain.com

# Basic Auth (Caddy)
# Put auth credentials in `./.env.caddy` (service-only, gitignored).
#
# See: `./.env.caddy.example`
